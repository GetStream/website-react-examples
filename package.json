{
  "name": "website-react-examples",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:GetStream/website-react-examples.git",
  "author": "Jaap <jaap@getstream.io>",
  "license": "MIT",
	"scripts": {
		"build:cs": "cd customer-support && yarn build && rm -rf ../docs/customer-support/* && mv build/* ../docs/customer-support",
		"build:sm": "cd social-messenger && yarn build && rm -rf ../docs/social-messenger/* && mv build/* ../docs/social-messenger",
		"build:ve": "cd virtual-event && yarn build && rm -rf ../docs/virtual-event/* && mv build/* ../docs/virtual-event",
		"build:all": "yarn build:cs && yarn build:sm && yarn build:ve"
	}
}
